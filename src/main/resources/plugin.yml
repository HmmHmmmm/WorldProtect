name: "${pom.artifactId}"
main: hmmhmmmm.worldprotect.WorldProtect
author: "HmmHmmmm"
version: "${pom.version}"
api: ["1.0.9"]
website: https://github.com/HmmHmmmm/WorldProtect

permissions:
  worldprotect:
    default: false
    children:
      worldprotect.command:
        default: op